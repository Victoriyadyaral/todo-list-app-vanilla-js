{{#each todos}}
<tr key={{this.id}}>
    <td>{{this.title}}</td>
    <td>{{this.created}}</td>
    <td>{{this.category}}</td>
    <td class="center">
        <button type="button"  class="unarchived-todo__btn btn" id={{this.id}}>
             <i class="material-icons unarchived" id='{{this.id}}-unarchiveBtn'>unarchive</i>
        </button>
    </td>
</tr>
{{/each}}