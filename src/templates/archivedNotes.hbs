{{#each todos}}
<tr key={{this.id}}>
    <td>{{this.title}}</td>
    <td>{{this.created}}</td>
    <td>{{this.category}}</td>
    <td>
        <button type="button" id='unarchiveBtn-{{this.id}}' class="unarchived-todo__btn button" id={{this.id}}>
             <i class="material-icons">unarchive</i>
        </button>
    </td>
</tr>
{{/each}}