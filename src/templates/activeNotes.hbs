{{#each todos}}
<tr key={{this.id}}>
    <td>    
    </td>
    <td>{{this.title}}</td>
    <td>{{this.created}}</td>
    <td>{{this.category}}</td>
    <td>{{this.content}}</td>
    <td>{{this.dates}}</td>
    <td class="center">
        <button type="button"  class="edit-todo__btn btn">
             <i class="material-icons edit" id='{{this.id}}-editBtn'>edit</i>
        </button>
    </td>
    <td class="center">
        <button type="button"  class="archived-todo__btn btn">
            <i class="material-icons archive" id='{{this.id}}-archiveBtn'>archive</i>
        </button>
    </td>
    <td class="center">
        <button type="button" class="delete-todo__btn btn">
            <i class="material-icons remove" id='{{this.id}}-deleteBtn'>remove</i>
        </button>
    </td>
</tr>
{{/each}}