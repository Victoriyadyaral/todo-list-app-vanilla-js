{{#each todos}}
<tr key={{this.id}}>
    <td>    
    </td>
    <td>{{this.title}}</td>
    <td>{{this.created}}</td>
    <td>{{this.category}}</td>
    <td>{{this.content}}</td>
    <td>{{this.dates}}</td>
    <td>
        <button type="button"  class="edit-todo__btn button">
             <i class="material-icons" id='{{this.id}}-editBtn'>edit</i>
        </button>
    </td>
    <td>
        <button type="button"  class="archived-todo__btn button">
            <i class="material-icons" id='{{this.id}}-archiveBtn'>archive</i>
        </button>
    </td>
    <td>
        <button type="button" class="delete-todo__btn button">
            <i class="material-icons" id='{{this.id}}-deleteBtn'>remove</i>
        </button>
    </td>
</tr>
{{/each}}